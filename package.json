{
  "name": "morning-cup-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that generates The Morning Cup daily podcast for The Penny Tribune.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250428.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.0",
    "wrangler": "^3.80.0"
  },
  "engines": {
    "node": ">=20"
  }
}
